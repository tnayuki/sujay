[package]
name = "sujay-library"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["rlib"]

[dependencies]
dirs = "5"
thiserror = "1"
rbox = { version = "=0.1.5", default-features = false, features = ["master-db", "anlz", "bundled-vendored-openssl"] }